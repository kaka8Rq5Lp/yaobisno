services:
  - type: web
    name: yaobisno
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
